---
- name: Install Ruby
  hosts: all
  become: true
  tasks:
    - name: Install required packages
      apt: "name={{ item }} state=present"
      with_items:
        - ruby-full
        - ruby-bundler
        - build-essential

